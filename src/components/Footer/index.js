import './footer.css'

const Footer = (props) => {
    return (
        <footer className={`footer ${props.colorLetter}`}>
            <p>2023, Yago Sousa</p>
        </footer>
    )
}

export default Footer 
